spring:
  r2dbc:
    url: r2dbc:sqlserver://localhost:1400/sqldb-ecom-gateway
    username: sa
    password: softweb#123
  jpa:
    hibernate:
      ddl-auto: create
  application:
    name: gateway-service
  cloud:
    compatibility-verifier:
      enabled: false
    gateway:
      discovery:
        locator:
          enabled: true

server:
  port: 80
  forward-headers-strategy: FRAMEWORK
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka


