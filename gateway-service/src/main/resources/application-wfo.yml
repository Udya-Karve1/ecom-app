spring:
  datasource:
    url: jdbc:sqlserver://localhost;databaseName=sqldb-ecom-gateway;encrypt=true;trustServerCertificate=true;
    username: sa
    password: softweb#123
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    hibernate:
      ddl-auto: update
  application:
    name: gateway-service
  cloud:
    compatibility-verifier:
      enabled: false
    gateway:
      discovery:
        locator:
          enabled: true
server:
  port: 80
  forward-headers-strategy: FRAMEWORK
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka


