<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/base.xml"/>

    <springProfile name="wfo">
        <appender name="fileRolling" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>../dr-cache-output.log</file>

            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <fileNamePattern>../dr-cache-output-%d{yyyy-MM-dd}.%i.log.zip</fileNamePattern>
                <maxFileSize>5MB</maxFileSize>
            </rollingPolicy>

            <encoder>
                <pattern>%d %p %c{1.} [%t] %m%n</pattern>
            </encoder>
        </appender>

        <root level="debug">
            <appender-ref ref="fileRolling"/>
        </root>
        <logger name="org.springframework.web" level="DEBUG"/>
    </springProfile>

</configuration>
